# Ace Translator：一个自用的轻量级桌面翻译工具

> 平时看英文文档、读 Issue、查报错，翻译是刚需。市面上优秀的翻译工具很多（比如 DeepL），不过我自己用起来总遇到点小插曲，索性自己写了一个。

## 下载地址

- [前往下载](https://translator.wangcaiyuan.com)

> (建议放一张动图：选中文字 -> 双击复制 -> 弹出翻译)

---

## 为什么重复造轮子？

其实主要是因为 DeepL 在我这边的体验不太稳定：
1. **速度偶尔慢半拍**，特别是高频查单词的时候，等待感挺明显的。
2. **macOS 端进程假死**，偶尔遇到点不开也关不掉的情况，只能强制退出。
3. **Windows 端的小毛病**，之前遇到过无法更新，想卸载重装又提示卸载失败，挺折腾人的。

想着既然现在 AI 翻译（像智谱、OpenAI）的 API 已经很成熟了，与其去修软件，不如花两个晚上自己写个顺手的“壳”。

本来是写给自己用的，主要追求一个**快**和**稳**。现在顺手分享出来，反正软件是免费的，代码也没啥复杂的，大家如果刚好也有类似痛点，可以试试看。

---

## 功能很简单

这个工具没啥花里胡哨的功能，核心逻辑就是：**不打断当前工作流，翻译结果随叫随到。**

### 1. 双击复制，直接翻译

不用切换窗口，不用打开浏览器。
- **macOS**：连续按两次 `Cmd + C`
- **Windows**：连续按两次 `Ctrl + C`

窗口会在鼠标旁边弹出来，看完如果不操作，鼠标移开或者点击别处它就自己消失了。

### 2. 多种唤起方式

除了双击复制，也保留了常规的唤起方式：
- `Alt/Option + Space`：在屏幕中间打开翻译框。
- 点击托盘图标：也能打开。

### 3. 关于模型和 Key

应用内置接入了智谱的 `glm-4-flash` 模型。

为了让大家下载就能试用，我内置了一个我自己的 API Key。不过因为这个免费模型有并发限制（好像是 2 并发），如果用的人多了可能会卡。

**建议大家自己申请一个 Key 填进去**（智谱的 Flash 模型目前是免费的），这样体验会稳很多。
- 填写位置：设置 -> 智谱 AI -> 填入 API Key

### 4. 数据存在本地

配置信息和翻译的历史记录都存在本地的 SQLite 数据库里。API Key 也是加密存在本地的。

---

## 快速上手

1. [下载安装](https://translator.wangcaiyuan.com)。
2. **macOS 用户留意一下**：
   - 第一次打开如果提示拦截，去「隐私与安全性」里点一下「仍要打开」。
   - 要想用双击复制功能，得在「隐私与安全性 -> 辅助功能」里给 Ace Translator 勾选一下权限（不然监听不到键盘事件）。

---

## 技术实现

给对技术感兴趣的朋友简单提一嘴，这其实就是个典型的 Tauri 应用：
- **UI**：Vue 3 + Tailwind CSS
- **壳**：Tauri 2 (Rust)
- **数据库**：SQLite

用 Rust 做后端是为了性能好一点，资源占用少一点。

---

## 碎碎念

这个工具肯定不如商业软件完善，毕竟只是我用了两个晚上搓出来的。有些功能可能还比较简陋，比如目前只接入了 AI 翻译，没有做 OCR 之类的。

如果你用着觉得还行，或者有什么显而易见的 Bug，欢迎在评论区告诉我。